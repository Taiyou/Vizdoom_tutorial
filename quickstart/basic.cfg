# Lines starting with # are treated as comments (or with whitespaces+#).
# It doesn't matter if you use capital letters or not.
# It doesn't matter if you use underscore or camel notation for keys, e.g. episode_timeout is the same as episodeTimeout.

# Skill 5 is reccomanded for the scenario to be a challenge.
doom_skill = 1

# Rewards
death_penalty = 100
living_reward = 0.01

# Rendering options
screen_resolution = RES_640X480
screen_format = RGB24
render_hud = false
render_crosshair = false
render_weapon = true
render_decals = false
render_particles = false
window_visible = true

render_effects_sprites = False
render_messages = False
render_corpses = False
render_screen_flashes = True  # Effect upon taking damage or picking up items

# Turns on the sound. (turned off by default)
sound_enabled = True

episode_start_time = 10
episode_timeout = 2100

# Available buttons
available_buttons = 
	{ 
		MOVE_LEFT 
		MOVE_RIGHT 
		ATTACK 
		#MOVE_FORWARD
		#MOVE_BACKWARD
		#TURN_LEFT
		#TURN_RIGHT
	}

# Game variables that will be in the state
available_game_variables = { AMMO2 }

mode = PLAYER


